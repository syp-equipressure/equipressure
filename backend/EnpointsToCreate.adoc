= API Vordefinition
== Klassendefinition 

* Person
** eine Person hat eine oder mehrere Rollen
*** Unterklasse für Rolle (kein eigener Endpunkt später)
** persons/devices

* CustomerRelationship
** hat Id für Kunde
** hat Id für Sattler
** hat boolean für isFavourite (einziger Grund warums die Klasse gibt)
** hat boolean für isContact

* Devices
** hat Person Besitzer
** hat List<Person> Nutzer
** hat DeviceCategory Id
*** Unterklasse DeviceCategory
** devices/persons


== Endpunkte

Personen:

* /persons => alle Personen
* /persons/:id => Person mit id :id
* /persons/:id/contacts => Kontakte einer Person
* /persons/:id/favourites => Favourisierte Sattler einer Person
* /persons/:id/devices => alle Geräte einer Person
** könnte eventuell boolean liefern ob Person Owner ist
* /persons/:id/horses => alle Pferde einer Person

Geräte:

* /devices/:personId => alle Geräte einer Person
* /devices/:id => Gerät mit der id :id
* /devices/:id/owner => Besitzer des Gerätes (Person)
* /devices/:id/users => alle Personen eines Geräts

Pferde: 

* /horses => alle Pferde
* /horses/:id => Pferd mit id :id
* /horses/:id/saddles => alle Sättel eines Pferdes
* /horses/:id/persons => Reitbeteiligungen + Owner
** Owner entweder an erster Stelle oder eigener Endpoint für Owner
** eventuell /horses/:id/owner

Messungen: 

* /measurementgroups => alle Messungsgruppen
* /measurementgoups/:id/measurements => Tabelle Measurement
* /measurementgroups/:id/measurements/:id => ein Measurement aus Tabelle Measurement
* /measurementgroups/:id/measurements/:id/measurementData/:id => ein MeasurementData aus Tabelle MeasurementData

== Fragen an die Professoren 
* Braucht man immer eine endpoint für "get all"
* Herr Professor Haslinger fragen, ob eigener Endpunkt nötig ist für roles bzw perons/:id/roles
* Könnte /horses/:id/owner sinnvoll sein? Oder string/int für name/id im Pferd selber